info:
  name: удаление товара
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/api/items/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
