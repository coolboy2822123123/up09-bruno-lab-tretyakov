info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/api/auth/register"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
