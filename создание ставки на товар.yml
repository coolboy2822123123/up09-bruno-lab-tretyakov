info:
  name: создание ставки на товар
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/items/:id/bids"
  params:
    - name: id
      value: "1773207628304"
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 850000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
